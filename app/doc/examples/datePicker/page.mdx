import EthiopianDatePickerWrapper from '../../../../components/EthiopianDatePickerWrapper'
import { FiCalendar, FiTool, FiCheckCircle } from 'react-icons/fi'
import { FaPalette } from 'react-icons/fa'

# <FiCalendar className="inline-block mr-2" /> Ethiopian Date Picker

<div className="flex justify-center my-8">
  <EthiopianDatePickerWrapper />
</div>

---

## <FiTool className="inline-block mr-2" /> How It Works

This date picker uses `kenat`'s `MonthGrid` and returns the selected Ethiopian date.

- <FiCalendar className="inline-block mr-1" /> Navigable month view
- <FiCheckCircle className="inline-block mr-1" /> Click to select any EC day
- <FaPalette className="inline-block mr-1" /> Styled for light/dark
