import { Tabs } from 'nextra/components'

# 📦 Installation

<Tabs items={['pnpm', 'npm', 'yarn', 'bun']}>
  <Tabs.Tab>
    ```bash
    pnpm add kenat
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    npm install kenat
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    yarn add kenat
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    bun add kenat
    ```
  </Tabs.Tab>
</Tabs>
