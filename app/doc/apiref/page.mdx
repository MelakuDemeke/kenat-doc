---
title: Kenat Class API
description: Full reference for the Kenat Ethiopian calendar class and its methods.
---

import { Tabs } from 'nextra/components'
import { SiJavascript, SiDart, SiPython, SiPhp } from 'react-icons/si'

# ⚙️ Kenat Class

The `Kenat` class provides a high-level API for working with Ethiopian calendar dates. You can create instances, format dates, manipulate them, and print calendars.

---

## 🏗 Constructor

`new Kenat(dateString?)`

Creates a new instance of an Ethiopian date.

- If no argument is passed, it uses the current Gregorian system date.
- If a string like `'2015/6/10'` is passed, it's parsed as an Ethiopian date.

<Tabs
  items={[
    <div key="js" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiJavascript size={16} /> JavaScript</div>,
    <div key="dart" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiDart size={16} /> Dart</div>,
    <div key="python" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPython size={16} /> Python</div>,
    <div key="php" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPhp size={16} /> PHP</div>
  ]}
>
  <Tabs.Tab>
    ```js
    const k = new Kenat();
    const custom = new Kenat('2016/6/10');
    ```
  </Tabs.Tab>
  <Tabs.Tab>🚧 Dart version coming soon...</Tabs.Tab>
  <Tabs.Tab>
    ```python
    from kenat import Kenat

    k = Kenat()
    custom = Kenat('2016/6/10')
    ```
  </Tabs.Tab>
  <Tabs.Tab>🚧 PHP version coming soon...</Tabs.Tab>
</Tabs>

---

## 📆 Date Access Methods

| Method            | Returns                          | Description                          |
|-------------------|-----------------------------------|--------------------------------------|
| `getEthiopian()` / `get_ethiopian()`  | `{ year, month, day }`            | Current Ethiopian date               |
| `getGregorian()` / `get_gregorian()`  | `{ year, month, day }`            | Equivalent Gregorian date            |
| `toString()` / `__str__()`      | `"Ethiopian: yyyy-mm-dd"`         | String format                        |
| `format(lang?)`   | `"መስከረም 5 2016"` or similar       | Human-readable string                |
| `formatInGeezAmharic()` / `format_in_geez_amharic()` | `"መስከረም ፭ ፳፻፲፮"`         | Geez-styled output                   |

<Tabs
  items={[
    <div key="js" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiJavascript size={16} /> JavaScript</div>,
    <div key="python" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPython size={16} /> Python</div>
  ]}
>
    <Tabs.Tab>
        ```js
        const k = new Kenat('2016/1/5');
        console.log(k.getEthiopian()); // { year: 2016, month: 1, day: 5 }
        console.log(k.toString()); // Ethiopian: 2016-1-5
        console.log(k.formatInGeezAmharic()); // መስከረም ፭ ፳፻፲፮
        ```
    </Tabs.Tab>
    <Tabs.Tab>
        ```python
        from kenat import Kenat

        k = Kenat('2016/1/5')
        print(k.get_ethiopian()) # {'year': 2016, 'month': 1, 'day': 5}
        print(str(k)) # Ethiopian: 2016-1-5
        print(k.format_in_geez_amharic()) # መስከረም ፭ ፳፻፲፮
        ```
    </Tabs.Tab>
</Tabs>


---

## 🕒 Time Handling

| Method               | Description                                        |
|----------------------|----------------------------------------------------|
| `getCurrentTime()` / `get_current_time()`   | Gets current time in Ethiopian format              |
| `setTime(hour, min, period)` / `set_time(hour, min, period)` | Sets Ethiopian time manually (e.g., 3:30 night)  |

<Tabs
  items={[
    <div key="js" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiJavascript size={16} /> JavaScript</div>,
    <div key="python" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPython size={16} /> Python</div>
  ]}
>
    <Tabs.Tab>
        ```js
        const k = new Kenat();
        console.log(k.getCurrentTime());
        ```
    </Tabs.Tab>
    <Tabs.Tab>
        ```python
        from kenat import Kenat

        k = Kenat()
        print(k.get_current_time())
        ```
    </Tabs.Tab>
</Tabs>

---

## ➕ Date Arithmetic

| Method              | Description                              |
|---------------------|------------------------------------------|
| `addDays(n)` / `add_days(n)`        | Move n days forward/backward             |
| `addMonths(n)` / `add_months(n)`      | Adjust by n Ethiopian months             |
| `addYears(n)` / `add_years(n)`       | Move by n Ethiopian years                |

<Tabs
  items={[
    <div key="js" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiJavascript size={16} /> JavaScript</div>,
    <div key="python" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPython size={16} /> Python</div>
  ]}
>
    <Tabs.Tab>
        ```js
        const k = new Kenat('2014/5/5');
        k.addDays(10);
        k.addMonths(-1);
        console.log(k.getEthiopian());
        ```
    </Tabs.Tab>
    <Tabs.Tab>
        ```python
        from kenat import Kenat

        k = Kenat('2014/5/5')
        k.add_days(10)
        k.add_months(-1)
        print(k.get_ethiopian())
        ```
    </Tabs.Tab>
</Tabs>

---

## 📏 Compare Dates

| Method                | Description      |
| --------------------- | ---------------- |
| `diffInDays(other)` / `diff_in_days(other)`   | Day difference   |
| `diffInMonths(other)` / `diff_in_months(other)` | Month difference |
| `diffInYears(other)` / `diff_in_years(other)`  | Year difference  |

<Tabs
  items={[
    <div key="js" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiJavascript size={16} /> JavaScript</div>,
    <div key="python" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPython size={16} /> Python</div>
  ]}
>
    <Tabs.Tab>
        ```js
        const a = new Kenat('2015/6/10');
        const b = new Kenat('2012/6/10');
        console.log(a.diffInYears(b)); // → 3
        ```
    </Tabs.Tab>
    <Tabs.Tab>
        ```python
        from kenat import Kenat

        a = Kenat('2015/6/10')
        b = Kenat('2012/6/10')
        print(a.diff_in_years(b)) # → 3
        ```
    </Tabs.Tab>
</Tabs>

---

## 🗓 Calendar Tools

| Method                     | Description                                  |
| -------------------------- | -------------------------------------------- |
| `getMonthCalendar()` / `get_month_calendar()`       | Full info for every day in the current month |
| `printThisMonth(useGeez?)` / `print_this_month(use_geez?)` | Console grid printer for current month       |

<Tabs
  items={[
    <div key="js" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiJavascript size={16} /> JavaScript</div>,
    <div key="python" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPython size={16} /> Python</div>
  ]}
>
    <Tabs.Tab>
        ```js
        const k = new Kenat('2016/1/1');
        k.printThisMonth(true);
        ```
    </Tabs.Tab>
    <Tabs.Tab>
        ```python
        from kenat import Kenat

        k = Kenat('2016/1/1')
        k.print_this_month(use_geez=True)
        ```
    </Tabs.Tab>
</Tabs>
