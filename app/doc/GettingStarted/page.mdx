---
title: Getting Started
description: Learn how to create and work with Ethiopian dates using Kenat.
---

import { Tabs } from 'nextra/components'
import { SiJavascript, SiDart, SiPython, SiPhp } from 'react-icons/si'

# 🚀 Getting Started

Kenat helps you create and manipulate Ethiopian dates using intuitive and language-consistent APIs.

It supports both current and custom dates, and lets you easily switch between Ethiopian and Gregorian formats.

---

## 📆 Create a New Date (Today’s Date)

<Tabs
  items={[
    <div key="js" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiJavascript size={16} /> JavaScript</div>,
    <div key="dart" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiDart size={16} /> Dart</div>,
    <div key="python" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPython size={16} /> Python</div>,
    <div key="php" style={{ display: 'flex', alignItems: 'center', gap: '0.4rem' }}><SiPhp size={16} /> PHP</div>
  ]}
>
  <Tabs.Tab>
    ```js
    import Kenat from 'kenat';

    const date = new Kenat(); // uses today's Gregorian date
    console.log(date.getEthiopian()); 
    // → { year: 2017, month: 9, day: 25 }

    console.log(date.getGregorian());
    // → { year: 2025, month: 6, day: 2 }
    ```
  </Tabs.Tab>

  <Tabs.Tab>
    🚧 Dart version coming soon...
  </Tabs.Tab>

  <Tabs.Tab>
    🚧 Python version coming soon...
  </Tabs.Tab>

  <Tabs.Tab>
    🚧 PHP version coming soon...
  </Tabs.Tab>
</Tabs>

---

## ✏️ Create from a Custom Ethiopian Date

```js
const custom = new Kenat('2016/6/10');
console.log(custom.getGregorian());
// → { year: 2024, month: 2, day: 18 }
